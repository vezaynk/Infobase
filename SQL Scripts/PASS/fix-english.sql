update "Activity"
set "ActivityNameEn" = fixed
from fixes
where "ActivityNameEn" = original;

update "Indicator"
set "IndicatorNameEn" = fixed
from fixes
where "IndicatorNameEn" = original;

update "IndicatorGroup"
set "IndicatorGroupNameEn" = fixed
from fixes
where "IndicatorGroupNameEn" = original;

update "LifeCourse"
set "LifeCourseNameEn" = fixed
from fixes
where "LifeCourseNameEn" = original;

update "Measure"
set "MeasureAdditionalRemarksEn" = fixed
from fixes
where "MeasureAdditionalRemarksEn" = original;

update "Measure"
set "MeasureDataAvailableEn" = fixed
from fixes
where "MeasureDataAvailableEn" = original;

update "Measure"
set "MeasureDefinitionEn" = fixed
from fixes
where "MeasureDefinitionEn" = original;

update "Measure"
set "MeasureMethodEn" = fixed
from fixes
where "MeasureMethodEn" = original;

update "Measure"
set "MeasureSourceLongEn" = fixed
from fixes
where "MeasureSourceLongEn" = original;

update "Measure"
set "MeasureSourceShortEn" = fixed
from fixes
where "MeasureSourceShortEn" = original;

update "Measure"
set "MeasureUnitLongEn" = fixed
from fixes
where "MeasureUnitLongEn" = original;

update "Measure"
set "MeasureUnitShortEn" = fixed
from fixes
where "MeasureUnitShortEn" = original;

update "Measure"
set "MeasureNameDataToolEn" = fixed
from fixes
where "MeasureNameDataToolEn" = original;

update "Measure"
set "MeasureNameIndexEn" = fixed
from fixes
where "MeasureNameIndexEn" = original;

update "Measure"
set "MeasurePopulationGroupEn" = fixed
from fixes
where "MeasurePopulationGroupEn" = original;

update "Point"
set "PointLabelEn" = fixed
from fixes
where "PointLabelEn" = original;

update "Point"
set "PointTextEn" = fixed
from fixes
where "PointTextEn" = original;

update "Strata"
set "StrataNameEn" = fixed
from fixes
where "StrataNameEn" = original;

update "Strata"
set "StrataNotesEn" = fixed
from fixes
where "StrataNotesEn" = original;

update "Strata"
set "StrataPopulationTitleFragmentEn" = fixed
from fixes
where "StrataPopulationTitleFragmentEn" = original;
update "Strata"

set "StrataSourceEn" = fixed
from fixes
where "StrataSourceEn" = original;