@model ReactDotNetDemo.Models.PASS.Strata

@{
    ViewData["Title"] = "Edit";
}

<h2>Edit</h2>

<h4>Strata</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="StrataId" />
            <div class="form-group">
                <label asp-for="MeasureId" class="control-label"></label>
                <select asp-for="MeasureId" class="form-control" asp-items="ViewBag.MeasureId"></select>
                <span asp-validation-for="MeasureId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
