@using model_generator;
@model MasterEntityModel
// This file was written by a tool
using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;
using model_generator;
using metadata_annotations;

namespace Infobase.Models.@(Model.DatasetName) {
    public class Master {
        public int Index { get; set; }
@foreach (var property in @Model.Properties) {
        @:[Text("@property", "en-ca")]
        @:[Text("@property", "fr-ca")]
        @:[CSV("@property")]
        @:public string @(Program.ToPascalCase(property)) { get; set; }
        @:
}
    }
}